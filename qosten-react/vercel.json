{
  "buildCommand": "npm install --legacy-peer-deps && CI=false npx react-scripts build",
  "env": {
    "CI": "false"
  },
  "outputDirectory": "build",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
